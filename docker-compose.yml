services:
  postgres:
    image: postgres:18
    container_name: postgres

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secret111
      POSTGRES_DB: nasrin_shop

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:var/lib/postgresql/data

volumes:
  postgres_data: